#!/usr/bin/env bash

set -eu

prelude () {
    cat $WD/sql/shots.sql \
        | sqlite3 -csv -header $WD/ruskie.db \
        > $WD/viz/data/shots.csv
}

postlude () {
    python $WD/viz/scripts/main.py
    open $WD/viz/out/shots.png
}

main () {
    prelude
    postlude
}

main
